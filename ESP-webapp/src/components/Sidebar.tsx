import React from 'react';

const sideBar: React.FC = () => {

    return (
        <div className="fixed top-0 left-0 h-screen w-16 flex flex-col bg-sky-200">
            This is a side bar.
        </div>
    );
}

export default sideBar;
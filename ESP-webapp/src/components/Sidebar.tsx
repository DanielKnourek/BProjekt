import React from "react";

const sideBar: React.FC = () => {
  return (
    <div className="fixed left-0 top-0 flex h-screen w-16 flex-col bg-sky-200">
      This is a side bar.
    </div>
  );
};

export default sideBar;
